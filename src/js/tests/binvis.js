
/*
QUnit.test("binvis.File", function (assert) {
	var curve = LineCurve;
	var p = {
		x: 0,
		y: 0
	};
	assert.equal(
		view_offset(p, 64, curve, 2, 8), 		
		0
	);

	p = {
		x: 1,
		y: 0
	};
	assert.equal(
		view_offset(p, 64, curve, 2, 8), 		
		4
	);
	p = {
		x: 0,
		y: 1
	};
	assert.equal(
		view_offset(p, 64, curve, 2, 8), 		
		8
	);

	p = {
		x: 1,
		y: 7
	};
	assert.equal(
		view_offset(p, 64, curve, 2, 8), 		
		60
	);
});
*/

